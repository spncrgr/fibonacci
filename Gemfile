source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :dev, :test do
  gem 'cucumber'
  gem 'minitest'
  gem 'pry-byebug'
  gem 'rake'
  gem 'rubocop'
end
